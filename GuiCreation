--[=[
 d888b  db    db d888888b      .d888b.      db      db    db  .d8b.  
88' Y8b 88    88   `88'        VP  `8D      88      88    88 d8' `8b 
88      88    88    88            odD'      88      88    88 88ooo88 
88  ooo 88    88    88          .88'        88      88    88 88~~~88 
88. ~8~ 88b  d88   .88.        j88.         88booo. 88b  d88 88   88    @uniquadev
 Y888P  ~Y8888P' Y888888P      888888D      Y88888P ~Y8888P' YP   YP  CONVERTER 
]=]

-- Instances: 25 | Scripts: 1 | Modules: 3 | Tags: 0
local G2L = {};

-- StarterGui.UiLibrary-
G2L["1"] = Instance.new("ScreenGui", game:GetService("Players").LocalPlayer:WaitForChild("PlayerGui"));
G2L["1"]["Name"] = [[UiLibrary-]];


-- StarterGui.UiLibrary-.UiHolder
G2L["2"] = Instance.new("Frame", G2L["1"]);
G2L["2"]["BorderSizePixel"] = 0;
G2L["2"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["2"]["AnchorPoint"] = Vector2.new(1, 1);
G2L["2"]["Size"] = UDim2.new(0.2, 1, 0.05, 1);
G2L["2"]["Position"] = UDim2.new(0.43317, 0, 0.40181, 0);
G2L["2"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["2"]["Name"] = [[UiHolder]];
G2L["2"]["BackgroundTransparency"] = 1;


-- StarterGui.UiLibrary-.UiHolder.Pattern
G2L["3"] = Instance.new("ImageLabel", G2L["2"]);
G2L["3"]["BorderSizePixel"] = 0;
G2L["3"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["3"]["ScaleType"] = Enum.ScaleType.Crop;
G2L["3"]["ImageTransparency"] = 0.15;
G2L["3"]["Image"] = [[rbxassetid://119444060294500]];
G2L["3"]["TileSize"] = UDim2.new(0.5, 0, 2, 0);
G2L["3"]["Size"] = UDim2.new(1, 1, 1, 1);
G2L["3"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["3"]["BackgroundTransparency"] = 1;
G2L["3"]["Name"] = [[Pattern]];


-- StarterGui.UiLibrary-.UiHolder.Pattern.UICorner
G2L["4"] = Instance.new("UICorner", G2L["3"]);



-- StarterGui.UiLibrary-.UiHolder.Pattern.UIStroke
G2L["5"] = Instance.new("UIStroke", G2L["3"]);
G2L["5"]["Thickness"] = 2;
G2L["5"]["Color"] = Color3.fromRGB(14, 4, 20);


-- StarterGui.UiLibrary-.UiHolder.TitleHolder
G2L["6"] = Instance.new("Frame", G2L["2"]);
G2L["6"]["BorderSizePixel"] = 0;
G2L["6"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["6"]["Size"] = UDim2.new(1.00459, 1, 0.71469, 1);
G2L["6"]["Position"] = UDim2.new(-0.00917, 1, -0.79743, 1);
G2L["6"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["6"]["Name"] = [[TitleHolder]];
G2L["6"]["BackgroundTransparency"] = 1;


-- StarterGui.UiLibrary-.UiHolder.TitleHolder.Title
G2L["7"] = Instance.new("TextLabel", G2L["6"]);
G2L["7"]["TextWrapped"] = true;
G2L["7"]["BorderSizePixel"] = 0;
G2L["7"]["TextSize"] = 80;
G2L["7"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["7"]["TextTransparency"] = 0.4;
G2L["7"]["TextYAlignment"] = Enum.TextYAlignment.Bottom;
G2L["7"]["TextScaled"] = true;
G2L["7"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["7"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["7"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["7"]["BackgroundTransparency"] = 1;
G2L["7"]["Size"] = UDim2.new(1, 1, 1, 1);
G2L["7"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["7"]["Text"] = [[Default Title]];
G2L["7"]["Name"] = [[Title]];


-- StarterGui.UiLibrary-.UiHolder.TitleHolder.Title.UITextSizeConstraint
G2L["8"] = Instance.new("UITextSizeConstraint", G2L["7"]);
G2L["8"]["MaxTextSize"] = 18;


-- StarterGui.UiLibrary-.UiHolder.TitleHolder.Title.UIStroke
G2L["9"] = Instance.new("UIStroke", G2L["7"]);
G2L["9"]["Transparency"] = 0.4;
G2L["9"]["Color"] = Color3.fromRGB(20, 14, 24);


-- StarterGui.UiLibrary-.UiHolder.SearchIcon
G2L["a"] = Instance.new("ImageLabel", G2L["2"]);
G2L["a"]["BorderSizePixel"] = 0;
G2L["a"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["a"]["ScaleType"] = Enum.ScaleType.Crop;
G2L["a"]["ImageTransparency"] = 0.5;
G2L["a"]["Image"] = [[rbxassetid://14942740976]];
G2L["a"]["Size"] = UDim2.new(0.16055, 1, 1, 1);
G2L["a"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["a"]["BackgroundTransparency"] = 1;
G2L["a"]["Name"] = [[SearchIcon]];


-- StarterGui.UiLibrary-.UiHolder.CommandBox
G2L["b"] = Instance.new("TextBox", G2L["2"]);
G2L["b"]["Name"] = [[CommandBox]];
G2L["b"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["b"]["ZIndex"] = 2;
G2L["b"]["BorderSizePixel"] = 0;
G2L["b"]["TextWrapped"] = true;
G2L["b"]["TextTransparency"] = 0.1;
G2L["b"]["TextSize"] = 80;
G2L["b"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["b"]["TextScaled"] = true;
G2L["b"]["BackgroundColor3"] = Color3.fromRGB(62, 46, 70);
G2L["b"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["b"]["Size"] = UDim2.new(0.65889, 1, 1, 1);
G2L["b"]["Position"] = UDim2.new(0.17597, 0, 0, 0);
G2L["b"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["b"]["Text"] = [[]];
G2L["b"]["BackgroundTransparency"] = 0.7;


-- StarterGui.UiLibrary-.UiHolder.CommandBox.UITextSizeConstraint
G2L["c"] = Instance.new("UITextSizeConstraint", G2L["b"]);
G2L["c"]["MaxTextSize"] = 24;


-- StarterGui.UiLibrary-.UiHolder.CommandBox.UIStroke
G2L["d"] = Instance.new("UIStroke", G2L["b"]);



-- StarterGui.UiLibrary-.UiHolder.CommandBox.UICorner
G2L["e"] = Instance.new("UICorner", G2L["b"]);
G2L["e"]["CornerRadius"] = UDim.new(0.02, 1);


-- StarterGui.UiLibrary-.UiHolder.CommandBox.UIStroke
G2L["f"] = Instance.new("UIStroke", G2L["b"]);
G2L["f"]["Transparency"] = 0.6;
G2L["f"]["ApplyStrokeMode"] = Enum.ApplyStrokeMode.Border;


-- StarterGui.UiLibrary-.UiHolder.SettingsButton
G2L["10"] = Instance.new("ImageButton", G2L["2"]);
G2L["10"]["BorderSizePixel"] = 0;
G2L["10"]["SliceScale"] = 2;
G2L["10"]["ScaleType"] = Enum.ScaleType.Crop;
G2L["10"]["ImageTransparency"] = 0.5;
G2L["10"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
G2L["10"]["Image"] = [[rbxassetid://14942807992]];
G2L["10"]["Size"] = UDim2.new(0.16055, 1, 1, 1);
G2L["10"]["BackgroundTransparency"] = 1;
G2L["10"]["Name"] = [[SettingsButton]];
G2L["10"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["10"]["Position"] = UDim2.new(0.83945, 0, -0.02853, 0);


-- StarterGui.UiLibrary-.UiHolder.UIScale
G2L["11"] = Instance.new("UIScale", G2L["2"]);
G2L["11"]["Scale"] = 0.5;


-- StarterGui.UiLibrary-.UiHolder.LocalScript
G2L["12"] = Instance.new("LocalScript", G2L["2"]);



-- StarterGui.UiLibrary-.UiHolder.LocalScript.Gossamer
G2L["13"] = Instance.new("ModuleScript", G2L["12"]);
G2L["13"]["Name"] = [[Gossamer]];


-- StarterGui.UiLibrary-.UiHolder.LocalScript.Maid
G2L["14"] = Instance.new("ModuleScript", G2L["12"]);
G2L["14"]["Name"] = [[Maid]];


-- StarterGui.UiLibrary-.UiHolder.LocalScript.Brio
G2L["15"] = Instance.new("ModuleScript", G2L["12"]);
G2L["15"]["Name"] = [[Brio]];


-- StarterGui.UiLibrary-.UiHolder.AutoComplete
G2L["16"] = Instance.new("TextLabel", G2L["2"]);
G2L["16"]["TextWrapped"] = true;
G2L["16"]["Active"] = true;
G2L["16"]["ZIndex"] = 2;
G2L["16"]["BorderSizePixel"] = 0;
G2L["16"]["TextSize"] = 80;
G2L["16"]["TextXAlignment"] = Enum.TextXAlignment.Left;
G2L["16"]["TextTransparency"] = 0.6;
G2L["16"]["TextScaled"] = true;
G2L["16"]["BackgroundColor3"] = Color3.fromRGB(62, 46, 70);
G2L["16"]["FontFace"] = Font.new([[rbxasset://fonts/families/SourceSansPro.json]], Enum.FontWeight.Regular, Enum.FontStyle.Normal);
G2L["16"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
G2L["16"]["BackgroundTransparency"] = 1;
G2L["16"]["Size"] = UDim2.new(0.65889, 1, 1, 1);
G2L["16"]["BorderColor3"] = Color3.fromRGB(0, 0, 0);
G2L["16"]["Text"] = [[Enter Command]];
G2L["16"]["Selectable"] = true;
G2L["16"]["Name"] = [[AutoComplete]];
G2L["16"]["Position"] = UDim2.new(0.17597, 0, 0, 0);


-- StarterGui.UiLibrary-.UiHolder.AutoComplete.UITextSizeConstraint
G2L["17"] = Instance.new("UITextSizeConstraint", G2L["16"]);
G2L["17"]["MaxTextSize"] = 24;


-- StarterGui.UiLibrary-.UiHolder.AutoComplete.UIStroke
G2L["18"] = Instance.new("UIStroke", G2L["16"]);



-- StarterGui.UiLibrary-.UiHolder.AutoComplete.UICorner
G2L["19"] = Instance.new("UICorner", G2L["16"]);
G2L["19"]["CornerRadius"] = UDim.new(0.02, 1);


-- Require G2L wrapper
local G2L_REQUIRE = require;
local G2L_MODULES = {};
local function require(Module:ModuleScript)
    local ModuleState = G2L_MODULES[Module];
    if ModuleState then
        if not ModuleState.Required then
            ModuleState.Required = true;
            ModuleState.Value = ModuleState.Closure();
        end
        return ModuleState.Value;
    end;
    return G2L_REQUIRE(Module);
end

G2L_MODULES[G2L["13"]] = {
Closure = function()
    local script = G2L["13"];local Maid = require(script.Parent.Maid)
local Brio = require(script.Parent.Brio)

local module = {}

local Log = { unknownTypes = {}, warnings = 0 }

local function calculateTransparency(orig, factor)
	return orig + (1 - orig) * factor
end

local function set(inst: Instance, prop: string, factor: number)
	if inst:GetAttribute(`CG_{prop}`) then
		inst[prop] = calculateTransparency(inst:GetAttribute(`CG_{prop}`), factor)
	end
end

local function handleInstance(instance: Instance, factor: number, doStrokes: boolean)
	if not instance:IsA("GuiObject") then
		Log.unknownTypes[instance.ClassName] = (Log.unknownTypes[instance.ClassName] or 0) + 1
		return
	end

	set(instance, 'BackgroundTransparency', factor)

	for _, child in ipairs(instance:GetChildren()) do
		if child:IsA("UIStroke") then
			set(child, 'Transparency', factor)
		end
	end

	if instance:IsA("TextLabel") or instance:IsA("TextButton") or instance:IsA("TextBox") then
		set(instance, 'TextTransparency', factor)
		if doStrokes then set(instance, 'TextStrokeTransparency', factor) end
	elseif instance:IsA("ImageButton") or instance:IsA("ImageLabel") then
		set(instance, 'ImageTransparency', factor)
	end
end

local function createValueBrio(parent: Instance, initialValue: number)
	local numberVal = Instance.new("NumberValue")
	numberVal.Name = "CanvasGroup"
	numberVal.Value = initialValue
	numberVal.Parent = parent

	local brio = Brio.new(numberVal)

	numberVal.Destroying:Connect(function()
		brio:Kill()
	end)

	return brio
end

function module:Create(parent, value, bool)
	local maid = Maid.new()
	local valueBrio = createValueBrio(parent, value)

	maid:GiveTask(function()
		valueBrio:Kill()
	end)

	local numberVal = valueBrio:GetValue()

	local function processDescendants(callback: (Instance) -> nil)
		local function process(inst)
			if not valueBrio:IsDead() then
				callback(inst)
			end
		end

		process(parent)
		for _, child in ipairs(parent:GetDescendants()) do
			process(child)
		end
	end

	processDescendants(function(inst)
		if inst:IsA("GuiObject") then
			inst:SetAttribute("CG_BackgroundTransparency", inst.BackgroundTransparency)
			if inst:IsA("TextLabel") or inst:IsA("TextButton") or inst:IsA("TextBox") then
				inst:SetAttribute("CG_TextTransparency", inst.TextTransparency)
				inst:SetAttribute("CG_TextStrokeTransparency", inst.TextStrokeTransparency)
			elseif inst:IsA("ImageButton") or inst:IsA("ImageLabel") then
				inst:SetAttribute("CG_ImageTransparency", inst.ImageTransparency)
			end
		elseif inst:IsA("UIStroke") then
			inst:SetAttribute("CG_Transparency", inst.Transparency)
		end
	end)

	local function updateAll()
		if valueBrio:IsDead() then return end
		processDescendants(function(inst)
			handleInstance(inst, numberVal.Value, bool)
		end)
	end

	maid:GiveTask(numberVal:GetPropertyChangedSignal("Value"):Connect(updateAll))
	maid:GiveTask(parent.DescendantAdded:Connect(function(inst)
		if not valueBrio:IsDead() and (inst:IsA("GuiObject") or inst:IsA("UIStroke")) then
			task.defer(updateAll)
		end
	end))

	updateAll()

	return numberVal, Log, maid, valueBrio
end

return module
end;
};
G2L_MODULES[G2L["14"]] = {
Closure = function()
    local script = G2L["14"];---	Manages the cleaning of events and other things.
-- Useful for encapsulating state and make deconstructors easy
-- @classmod Maid
-- @see Signal

local Maid = {}
Maid.ClassName = "Maid"

--- Returns a new Maid object
-- @constructor Maid.new()
-- @treturn Maid
function Maid.new()
	return setmetatable({
		_tasks = {}
	}, Maid)
end

function Maid.isMaid(value)
	return type(value) == "table" and value.ClassName == "Maid"
end

--- Returns Maid[key] if not part of Maid metatable
-- @return Maid[key] value
function Maid:__index(index)
	if Maid[index] then
		return Maid[index]
	else
		return self._tasks[index]
	end
end

--- Add a task to clean up. Tasks given to a maid will be cleaned when
--  maid[index] is set to a different value.
-- @usage
-- Maid[key] = (function)         Adds a task to perform
-- Maid[key] = (event connection) Manages an event connection
-- Maid[key] = (Maid)             Maids can act as an event connection, allowing a Maid to have other maids to clean up.
-- Maid[key] = (Object)           Maids can cleanup objects with a `Destroy` method
-- Maid[key] = nil                Removes a named task. If the task is an event, it is disconnected. If it is an object,
--                                it is destroyed.
function Maid:__newindex(index, newTask)
	if Maid[index] ~= nil then
		error(("'%s' is reserved"):format(tostring(index)), 2)
	end

	local tasks = self._tasks
	local oldTask = tasks[index]

	if oldTask == newTask then
		return
	end

	tasks[index] = newTask

	if oldTask then
		if type(oldTask) == "function" then
			oldTask()
		elseif typeof(oldTask) == "RBXScriptConnection" then
			oldTask:Disconnect()
		elseif oldTask.Destroy then
			oldTask:Destroy()
		end
	end
end

--- Same as indexing, but uses an incremented number as a key.
-- @param task An item to clean
-- @treturn number taskId
function Maid:GiveTask(task)
	if not task then
		error("Task cannot be false or nil", 2)
	end

	local taskId = #self._tasks+1
	self[taskId] = task

	if type(task) == "table" and (not task.Destroy) then
		warn("[Maid.GiveTask] - Gave table task without .Destroy\n\n" .. debug.traceback())
	end

	return taskId
end

function Maid:GivePromise(promise)
	if not promise:IsPending() then
		return promise
	end

	local newPromise = promise.resolved(promise)
	local id = self:GiveTask(newPromise)

	-- Ensure GC
	newPromise:Finally(function()
		self[id] = nil
	end)

	return newPromise
end

--- Cleans up all tasks.
-- @alias Destroy
function Maid:DoCleaning()
	local tasks = self._tasks

	-- Disconnect all events first as we know this is safe
	for index, task in pairs(tasks) do
		if typeof(task) == "RBXScriptConnection" then
			tasks[index] = nil
			task:Disconnect()
		end
	end

	-- Clear out tasks table completely, even if clean up tasks add more tasks to the maid
	local index, task = next(tasks)
	while task ~= nil do
		tasks[index] = nil
		if type(task) == "function" then
			task()
		elseif typeof(task) == "RBXScriptConnection" then
			task:Disconnect()
		elseif task.Destroy then
			task:Destroy()
		end
		index, task = next(tasks)
	end
end

--- Alias for DoCleaning()
-- @function Destroy
Maid.Destroy = Maid.DoCleaning

return Maid
end;
};
G2L_MODULES[G2L["15"]] = {
Closure = function()
    local script = G2L["15"];--- Brios are wrap a value and provide the following constraints
--[[
- Can be in 2 states, dead or alive
- While alive, can retrieve values
- While dead, retrieving values is forbidden
- Died will fire once upon death

Brios encapsulate the "lifetime" of a valid resource. Unlike a maid, they
- Can only die once
- Have less memory leaks
- Cannot be reentered

Calling :Destroy() or :Kill() after death does nothing. Brios cannot be resurrected.

Useful for downstream events where you want to emit a resource. Typically
brios should be killed when their source is killed.

Brios are intended to be merged with downstream brios so create a chain of reliable
resources.

Anything may "kill" a brio by calling :Destroy() or :Kill().
]]
-- @classmod Brio
local Maid = require(script.Parent:WaitForChild("Maid"))
local Brio = {}
Brio.ClassName = "Brio"
Brio.__index = Brio

function Brio.isBrio(value)
	return type(value) == "table" and value.ClassName == "Brio"
end

function Brio.new(...) -- Wrap
	return setmetatable({
		_values = table.pack(...);
	}, Brio)
end

function Brio:GetDiedSignal()
	if self:IsDead() then
		error("Already dead")
	end

	if self._diedEvent then
		return self._diedEvent.Event
	end

	self._diedEvent = Instance.new("BindableEvent")
	return self._diedEvent.Event
end

function Brio:IsDead()
	return self._values == nil
end

function Brio:ErrorIfDead()
	if not self._values then
		error("[CancelToken.ErrorIfDead] - Dead")
	end
end

function Brio:ToMaid()
	assert(self._values ~= nil, "Dead")

	local maid = Maid.new()

	maid:GiveTask(self:GetDiedSignal():Connect(function()
		maid:DoCleaning()
	end))

	return maid
end

function Brio:GetValue()
	assert(self._values)

	return unpack(self._values, 1, self._values.n)
end

function Brio:Destroy()
	if not self._values then
		return
	end

	self._values = nil

	if self._diedEvent then
		self._diedEvent:Fire()
		self._diedEvent:Destroy()
		self._diedEvent = nil
	end
end
Brio.Kill = Brio.Destroy

-- Setup dead
Brio.DEAD = Brio.new()
Brio.DEAD:Kill()

return Brio
end;
};
-- StarterGui.UiLibrary-.UiHolder.LocalScript
local function C_12()
local script = G2L["12"];
	local Gossamer = require(script.Gossamer)
	local Group = Gossamer:Create(script.Parent,0,true)
end;
task.spawn(C_12);

return G2L["1"], require;
